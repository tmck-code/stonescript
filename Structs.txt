func DictLookup(dict, k)
  for i = 0 .. dict.Count()-1
    if dict[i][0] = k
      return dict[i][1]
  return "not found"

var d = [
  ["a", [1]],
  ["b", [2]],
  ["c", [3]],
]
// >20,0,lookup: @d@, "a" -> @DictLookup(d, "a")@
